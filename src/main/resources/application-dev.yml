spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.43.104:3306/blog?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.qianluohan: debug
  file:
    name: log/blog.log
server:
  port: 8080
